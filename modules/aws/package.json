{
  "name": "aws",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "deploy": "npm-run-all deploy:src deploy:cfn",
    "deploy:src": "./tools/deploy-src",
    "deploy:cfn": "./tools/deploy-cfn",
    "stack:delete": "aws cloudformation delete-stack --stack-name $CFN_STACK_NAME",
    "stack:describe": "aws cloudformation describe-stack-events --stack-name $CFN_STACK_NAME"
  },
  "dependencies": {
    "kuromoji": "^0.1.2"
  }
}
